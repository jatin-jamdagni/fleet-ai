/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind v4: prefer CSS-first theming in src/index.css via @theme.
  // Keep this file only for optional future plugins.
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {},
  plugins: [],
};
